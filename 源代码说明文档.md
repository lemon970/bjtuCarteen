# 源代码说明文档

本文档用于说明当前项目目录下各个子目录、源代码文件和其他配套文件的用途，便于提交、答辩和后续维护。文档以 `src_24281231` 为根目录（Spring Boot 后端 + React 前端 + C++ 分析子系统的整合工程）。

## 1. 项目整体说明

本项目是一个食堂高峰客流仿真系统，包含三个独立但协作的子系统：

- Java 后端：基于 Spring Boot 的离散事件仿真引擎，提供同步 / 异步运行接口、报告读写、场景预设、瓶颈诊断与高级统计代理。
- React 前端：基于 Vite + Tailwind + ECharts，承载参数输入、结果展示和分析面板。
- C++ 分析子系统：通过 `ProcessBuilder` 调用，做 Bootstrap 置信区间和 Gini 瓶颈打分。binary 缺失时由 Java `InternalStatisticsAnalyzer` 输出等价 schema。

当前主要技术栈：

| 类型 | 内容 |
| --- | --- |
| 后端语言 | Java 17 |
| 后端框架 | Spring Boot 3.3.4 |
| Web 接口 | Spring MVC |
| 参数校验 | Spring Validation |
| JSON 序列化 | Jackson |
| 构建工具 | Maven + frontend-maven-plugin |
| 测试框架 | JUnit 5、Spring Boot Test、MockMvc |
| 前端语言 | JavaScript (ESM) |
| 前端框架 | React 18 + Vite 5 |
| 前端测试 | Vitest |
| C++ 工具链 | MSBuild / Project3.sln |

## 2. 子目录说明

| 目录 | 说明 |
| --- | --- |
| `src/main/java` | 后端 Java 源代码目录。 |
| `src/main/java/com/bjtu/simulation` | 项目主包。 |
| `src/main/java/com/bjtu/simulation/controller` | Web 控制层，承载 `/api/simulation/**` 与 `/api/analysis/**` 路由和全局异常处理。 |
| `src/main/java/com/bjtu/simulation/dto` | 数据传输对象层，定义请求配置、响应报告、统计结果、瓶颈诊断、窗口选择诊断等结构。 |
| `src/main/java/com/bjtu/simulation/engine` | 仿真引擎层，包含事件队列、随机采样、学生画像、窗口选择、不变量校验和快照记录。 |
| `src/main/java/com/bjtu/simulation/model` | 仿真领域模型层。 |
| `src/main/java/com/bjtu/simulation/service` | 业务服务层，封装配置归一化、仿真运行、报告持久化、异步任务、时间线构建、瓶颈分析、历史诊断等逻辑。 |
| `src/main/resources` | Spring Boot 资源目录，包含配置文件和静态前端产物。 |
| `src/main/resources/static/frontend` | 前端构建产物（`npm run build:backend` 写入），打包随 jar 一起发布。 |
| `src/test/java` | 自动化测试源码目录。 |
| `sun/` | React 前端源代码与开发依赖。 |
| `dataAnalyze/` | C++ 高级统计后处理子系统（CMake/MSBuild 构建，可选）。 |
| `examples/` | 示例请求体（`scenarios/canteen-scenario-set.json` 等）。 |
| `reports/` | 仿真报告 JSON 落盘目录（运行期生成，仅 `.gitkeep` 入库）。 |

## 3. 主程序文件说明

| 文件 | 说明 |
| --- | --- |
| `src/main/java/com/bjtu/simulation/SimulationApplication.java` | Spring Boot 应用启动入口。 |
| `src/main/java/com/bjtu/simulation/config/AppBeansConfig.java` | 应用级 Bean 装配；通过 `createReportObjectMapper()` 静态工厂统一提供 Jackson `ObjectMapper`，避免 `@ConditionalOnMissingBean` 关闭默认 mapper。 |

## 4. 控制层文件说明

| 文件 | 说明 |
| --- | --- |
| `controller/SimulationController.java` | 同步/异步仿真入口。提供 `POST /api/simulation/run` 同步运行、`POST /api/simulation/run/async` 异步提交、`GET /api/simulation/task/{id}/status` 状态查询。 |
| `controller/SimulationReportController.java` | 报告读取入口。提供 `GET /api/simulation/report/latest`、`/report/{id}`、`/report/{id}/csv` 导出。 |
| `controller/SimulationScenarioController.java` | 场景预设入口。提供 `GET /api/simulation/scenarios` 与 `POST /api/simulation/scenarios/run`。 |
| `controller/AnalysisController.java` | 高级统计入口。提供 `POST /api/analysis/run`，内部调 C++ binary 或 Java fallback；可选合并历史诊断 / 历史质量子树。 |
| `controller/FrontendController.java` | `/frontend` 路径转发，方便集成构建后访问前端入口页。 |
| `controller/ReportResponseBuilder.java` | 响应裁剪器。按 `include_history` 决定是否剥除 `history` 等大字段。 |
| `controller/SimulationApiSupport.java` | Controller 层共享的 mapper 工厂与轻量工具方法。 |
| `controller/GlobalExceptionHandler.java` | 全局异常处理器，统一包装为 `ApiResponse`。 |

## 5. DTO 文件说明

| 文件 | 说明 |
| --- | --- |
| `dto/ApiResponse.java` | 统一接口响应包装类，结构为 `code` / `message` / `data`。 |
| `dto/SimConfig.java` | 仿真配置对象。包含基础窗口/座位/到达率，分布配置、打包概率、波峰、天气、群组等参数。 |
| `dto/SimulationReport.java` | 完整仿真报告对象，含报告版本、报告 ID、有效种子、归一化后的配置、统计 summary、生成时间。 |
| `dto/SimulationSummary.java` | 仿真统计汇总对象。除人数/等待/座位/窗口字段外，包含 `window_choice_metrics`（仅 `PREFERENCE_AWARE` 输出）和 `bottleneck_diagnosis` 子 DTO。 |
| `dto/SimulationResult.java` | 仿真历史事件快照对象，记录某一事件发生后的细粒度回放数据。 |
| `dto/SimulationTimePoint.java` | 分钟级时间线快照，前端图表渲染用。 |
| `dto/WaitTimeMetrics.java` `WaitTimeBucket.java` `WaitTimeInsight.java` | 等待时间指标、分桶、归因结论。 |
| `dto/ProbabilityModelSummary.java` | 到达分布、间隔分布、服务分布等分布 metadata 摘要。 |
| `dto/TakeawayRateBreakdown.java` | 打包率三段分解：初始意图 / 动态翻转 / 无座强制。 |
| `dto/QueueChoiceModel.java` | 队列选择模型枚举（`STATIC_SPLIT` / `PREFERENCE_AWARE`）。 |
| `dto/WindowAttractivenessConfig.java` | RFC-009 窗口吸引力配置（popular / normal / cold 比例与权重）。 |
| `dto/WindowChoiceMetrics.java` | RFC-009 PR-9D 派生诊断，仅 `PREFERENCE_AWARE` 模式输出。 |
| `dto/BottleneckType.java` `BottleneckSeverity.java` `BottleneckEvidence.java` `DetectedBottleneck.java` `BottleneckDiagnosis.java` | RFC-012 派生瓶颈诊断 5 个 enum / 不可变 POJO。 |
| `dto/FrameSeatLayout.java` | 时间线帧内每张桌子的紧凑结构，前端时间轴回放用。 |
| `dto/ScenarioPreset.java` `ScenarioExpectedMetrics.java` `ScenarioBatchRunRequest.java` | 场景预设的元信息、期望指标和批量运行请求体。 |

## 6. 仿真引擎文件说明

| 文件 | 说明 |
| --- | --- |
| `engine/BaseEvent.java` | 仿真事件抽象基类，定义事件时间、优先级与排序。 |
| `engine/SimulationEngine.java` | 仿真核心引擎。维护事件优先队列、随机数生成器、状态、学生与统计指标，调度并执行全部事件。 |
| `engine/CanteenState.java` | 食堂运行状态对象，维护窗口队列、座位占用、桌位区域。 |
| `engine/StudentArriveEvent.java` `MovementEvent.java` `ServiceFinishEvent.java` `StudentLeaveEvent.java` `SeatWaitEvent.java` | 学生生命周期 5 个核心事件类。 |
| `engine/WindowSelectionPolicy.java` | 窗口选择策略。按 `queueSize + preferencePenalty + delayPenalty + windowTypePenalty` 综合打分，含打包窗口优先逻辑。 |
| `engine/TakeawayDecisionPolicy.java` | 服务完成时的打包决策策略，叠加天气因子、队列压力与座位压力。 |
| `engine/StudentProfileFactory.java` | 学生画像工厂，按到达类型生成 `windowPreference`、耐心、打包意图等属性。 |
| `engine/WindowRoleAssigner.java` `WindowRole.java` | RFC-009 窗口角色（POPULAR/NORMAL/COLD）静态分配。 |
| `engine/WindowAttractivenessSampler.java` | RFC-009 PR-9C cumulative-weight 抽样器。 |
| `engine/WindowChoiceMetricsBuilder.java` | RFC-009 PR-9D `window_choice_metrics` 在 `PREFERENCE_AWARE` 模式末态聚合。 |
| `engine/SimulationRandomSampler.java` | 随机采样集中入口（到达间隔 / 服务时长 / 用餐时长等）。 |
| `engine/SimulationDurationPolicy.java` | 仿真时长策略。 |
| `engine/SimulationInvariantChecker.java` | 不变量检查（人数守恒、状态合法性等）。 |
| `engine/SimulationSnapshotRecorder.java` | 引擎内部的逐帧快照记录器。 |

## 7. 领域模型文件说明

| 文件 | 说明 |
| --- | --- |
| `model/Student.java` | 学生实体，含 ID、打包偏好、耐心、窗口偏好、找座耐心、到达类型、群组、状态机、关键时间戳等。 |
| `model/StudentState.java` | 学生状态枚举（排队/服务/找座/就餐/离开）。 |
| `model/ArrivalGroup.java` | 到达类型枚举（普通 / 下课波峰 / 雨天驱动）。 |
| `model/ArrivalSample.java` `TakeawayDecisionRecord.java` `WaitTimeSample.java` | 运行期采样记录，分别用于到达样本、打包决策样本和等待时间样本。 |
| `model/DiningArea.java` | 就餐区模型，负责桌位构建、单人/多人占座、桌位利用时长统计与桌位快照生成。 |
| `model/TableSnapshot.java` `SeatCellSnapshot.java` | 桌位与座位级快照对象。 |

## 8. 服务层文件说明

| 文件 | 说明 |
| --- | --- |
| `service/SimulationRunService.java` | 同步仿真运行服务。负责配置归一化、创建引擎、调度到达事件、执行仿真、构建 summary，并把 `WindowChoiceMetrics` 与 `BottleneckDiagnosis` 通过 setter 注入。 |
| `service/SimulationConfigNormalizer.java` | 配置归一化与校验。补默认值、处理分布配置、校验窗口与座位、概率范围等。 |
| `service/SimulationArrivalScheduler.java` | 到达事件调度服务，按总时长、到达率、波峰与天气写入到达事件。 |
| `service/SimulationTimelineBuilder.java` | 时间线构建服务，把事件级 `history` 压缩成分钟级 `timeline`。 |
| `service/SimulationReportRepository.java` | 报告文件仓库。负责写入本地 JSON、读取 latest、按 ID 查询，并对 `report_id` 做安全校验（仅字母/数字/下划线/横线/点号）。 |
| `service/SimulationTaskService.java` `SimulationTaskRecord.java` | 异步任务服务。负责提交后台仿真任务、维护任务表、生成 snapshot；TTL 30 分钟，任务上限 200。 |
| `service/ScenarioPresetCatalog.java` `ScenarioRunService.java` | 6 套预设模型目录与批量运行服务，支持 `include_history` 和 `overrides`。 |
| `service/WaitTimeMetricsCalculator.java` | 等待时间指标计算（典型等待、百分位、分布、归因 insight）。 |
| `service/BottleneckAnalyzer.java` | RFC-012 派生瓶颈诊断纯函数，输入 `SimulationSummary` + `SimConfig`，闭式分发 4 类瓶颈 + BALANCED 兜底。 |
| `service/ExternalAnalysisService.java` | 调用 C++ binary（`ProcessBuilder` + 30s 超时），输入已落盘的报告 JSON，输出统计结果；缺失时回落到 Java 实现。 |
| `service/InternalStatisticsAnalyzer.java` | C++ binary 缺失时的 Java fallback，输出与 C++ 同 schema 的 `confidence_intervals` / `bottleneck` / `headline_metrics`。 |
| `service/HistoricalDiagnosticsService.java` `HistoricalQualityScorer.java` | 高级分析的可选历史诊断 / 历史质量打分子树，仅当请求体显式带 `include_historical_*` flag 时合并到响应中。 |
| `service/ReportSummaryStore.java` `ReportSummaryExtractor.java` | 报告 summary 的轻量提取器与缓存接口，供历史诊断服务消费。 |
| `service/SimulationMath.java` | 仿真层共享数学工具（`round3` / `clamp` / `rate` / 百分位等）。 |
| `service/WeatherFactorPolicy.java` | 天气因子策略，把 `weather_type × weather_impact_factor` 解析成有效因子。 |

## 9. 资源文件说明

| 文件 | 说明 |
| --- | --- |
| `src/main/resources/application.yml` | Spring Boot 配置文件，启用 HTTP 响应压缩，并配置 Jackson 默认忽略空字段。 |
| `src/main/resources/static/frontend/` | React 前端生产产物（`npm run build:backend` 写入；运行 `mvn spring-boot:run` 时随 jar 一起发布到 `/frontend/` 路径）。 |

## 10. 测试文件说明

测试目录 `src/test/java` 下覆盖单元、契约、集成三个层面，验证以下方面：

- 控制层契约：参数校验、HTTP 响应结构、snake_case / camelCase 双兼容、scenarios 批量响应、async 任务生命周期、analysis 降级语义。
- 引擎核心：固定 seed 决定论、零流量边界、队列上限放弃、波峰叠加、雨天联动、打包窗口路由、座位释放、群组到达、时间线帧布局。
- 服务层：配置归一化、theoretical takeaway baseline、bottleneck analyzer 4 类瓶颈与 BALANCED 兜底、weather factor policy。

## 11. 根目录其他文件说明

| 文件 | 说明 |
| --- | --- |
| `pom.xml` | Maven 项目配置文件。声明依赖、Java 版本与 `frontend-maven-plugin` 集成构建（`-DskipFrontend=true` 可跳过前端构建）。 |
| `start.ps1` | Windows PowerShell 启动脚本。支持指定端口、跳过构建、前台运行；默认会构建项目、检查端口占用、启动 jar 并写日志。 |
| `README.md` | 项目入口说明，含快速启动、场景模型与分析子系统接口示例。 |
| `API.md` | API 设计与接口字段定义。 |
| `ARCHITECTURE.md` | 模块结构、数据流图与设计约束。 |
| `METRICS.md` | 指标定义文档，记录人数、到达模型、等待时间、座位、窗口吞吐量、timeline 与高级统计输出口径。 |
| `USER_GUIDE.md` | 前端用户操作指南。 |
| `.editorconfig` | 编辑器格式约束。 |

## 12. 不应提交或可再生成的文件说明

| 路径 | 说明 |
| --- | --- |
| `target/` | Maven 编译/测试/打包产物，可由 `mvn test` / `mvn package` 重新生成。 |
| `reports/` | 仿真运行生成的 JSON 报告缓存，可由接口重新生成（仅 `.gitkeep` 入库）。 |
| `run/` | 启动脚本产生的 pid 文件和运行日志。 |
| `sun/node_modules/` | 前端依赖，可由 `npm install` 重新生成。 |
| `tmp_report/` | 生成单元测试 PDF 报告时产生的临时渲染文件和中间 PDF。 |

## 13. 代码阅读建议

建议按以下顺序阅读源代码：

1. 先读 `SimulationApplication.java` 与 `controller/` 下 4 个 `*Controller`，了解系统入口与对外接口。
2. 再读 `SimConfig.java` `SimulationReport.java` `SimulationSummary.java`，理解输入配置与输出报告结构（含 `window_choice_metrics` / `bottleneck_diagnosis` 两个可选子树）。
3. 然后读 `SimulationRunService.java` `SimulationConfigNormalizer.java` `SimulationArrivalScheduler.java`，理解一次仿真如何被准备和启动。
4. 接着读 `SimulationEngine.java` 与 `engine/` 下事件类和 RFC-009 PR-9C/9D 系列窗口选择类，理解离散事件仿真的执行流程。
5. 读 `service/BottleneckAnalyzer.java` 与 `service/InternalStatisticsAnalyzer.java`，理解派生诊断与高级统计的纯函数实现。
6. 最后读 `controller/` 下集成测试与 `service/` 下契约测试，通过测试用例确认接口契约和核心业务规则。
