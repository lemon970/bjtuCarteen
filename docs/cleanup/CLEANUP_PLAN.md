# CLEANUP_PLAN.md

> 阶段 2 删除计划。基于 `CLEANUP_AUDIT.md` 的证据,按 P0/P1/P2/P3 分组。
> **本文件只是计划,不会自动执行**。等待用户审批后再进 PR-CLEAN-1。

---

## 0. 执行原则

1. 每条删除项必须能在 `CLEANUP_AUDIT.md` 找到 grep 证据。
2. 每个 PR 完成后必须跑对应基线测试:`mvn -DskipFrontend=true test`、`cd sun && npm test -- --run`、`cd sun && npm run build:backend`,**全绿**才进下一个 PR。
3. P0 与 P1 列出的项默认执行,P2 中"建议执行"的默认执行,P3 暂缓项**等待用户裁决**(见 §6)。
4. 所有 git 操作走 `git rm`(从索引移除),物理目录用 `rm -rf`。**不**修改 git 历史。**不**做 force push。

---

## 1. PR-CLEAN-1:构建产物 / 运行产物(无争议)

### 1.1 操作

```bash
cd /d/desktop/stu/software/src_24281231

# A) target/ 跟踪文件 59 个 → 移出索引(.gitignore 已含)
git rm -r --cached target

# B) 前端 build 产物 4 个 → 移出索引并加 .gitignore
git rm --cached \
    src/main/resources/static/frontend/index.html \
    src/main/resources/static/frontend/assets/index-BEfG-hW-.css \
    src/main/resources/static/frontend/assets/index-DaHL9keg.js \
    src/main/resources/static/index.html

# C) VS .user 私有文件 → 移出索引(.gitignore 已含 *.vcxproj.user)
git rm --cached dataAnalyze/Project3.vcxproj.user

# D) 物理删除运行产物(已 gitignore,占盘 ~870M)
rm -rf target reports analysis-store sun/dist
rm -rf dataAnalyze/.vs dataAnalyze/Project3/x64

# E) .gitignore 加规则
#    /src/main/resources/static/frontend/
#    /src/main/resources/static/index.html
```

### 1.2 .gitignore 增量(本 PR 内 Edit)

在已有规则后追加:

```
# Frontend bundle output (regenerated by `npm run build:backend`)
/src/main/resources/static/frontend/
/src/main/resources/static/index.html
```

### 1.3 验收

```bash
mvn -DskipFrontend=true test                    # 仍 468/0/0
cd sun && npm test -- --run                     # 仍 62/0/0
cd sun && npm run build:backend                 # 重生成 static/frontend/* (这些会被新 .gitignore 拦截)
git status --short                              # 应只显示新写的 .gitignore 改动 + cleanup 文档
git ls-files | grep -E '^target/|/x64/|/Debug/|/Release/|\.class$|\.exe$|\.obj$|\.log$|\.pid$|simulation_report_.*\.json' || true
                                                # 期望:0 输出
```

### 1.4 估算

- **删除文件数(从索引)**:59 + 4 + 1 = **64 个**
- **物理空间回收**:~870 MB
- **零代码改动**

### 1.5 风险

- `ServedFrontendBundleFreshnessTest` 依赖 `static/frontend/` 存在 → 在跑 mvn test 前先跑 `npm run build:backend` 重生成。
- 若 mvn 配的 `frontend-maven-plugin` 在 `generate-resources` 阶段会自动跑 build:backend,则无需手动。本仓库 pom.xml §47-99 已配,但本仓库 mvn test 时用 `-DskipFrontend=true`,所以**必须先手动 npm build**。**收尾步骤把这个写进 PR 描述**。
- 如果 `git rm --cached` 后基线 mvn test 因 `static/frontend/` 缺失而失败 → 先跑 `npm run build:backend` 再跑 mvn test。

---

## 2. PR-CLEAN-2:废弃后端源码

**结论:本 PR 跳过(无安全可删的后端源码)。**

理由:`CLEANUP_AUDIT.md §4.2` 与 §4.4 已确认 — 113 个 main + 59 个 test 全部有引用证据,无孤儿。Optimize sync 路径(§4.5)是 P3 暂缓,等用户裁决。

如用户在 §6 选择删除 sync optimize 路径,**才**进入本 PR。具体操作放 §6.A。

---

## 3. PR-CLEAN-3:废弃前端代码

**结论:本 PR 跳过(无废弃前端代码)。**

`CLEANUP_AUDIT.md §3.1-3.2` 已证明 21 个 jsx + 5 个 utils + 12 个 test 全在 App.jsx 引用图内。

---

## 4. PR-CLEAN-4:文档清理

### 4.1 P1 默认删除(2 文件)

| 文件 | 大小估 | 删除依据 |
|---|---|---|
| `IMPLEMENTATION_SPLIT.md` | "Final Phase 四人协作产出对应",一次性历史阶段记录 | clean.txt §3.1 "已被代码实现替代的设计文档"+"开发过程交接文档" |
| `FRONTEND_REFACTOR_REPORT.md` | "前端中文多界面重构与接口对齐说明",一次性重构报告 | clean.txt §3.1 "临时审查报告" |

操作:`git rm IMPLEMENTATION_SPLIT.md FRONTEND_REFACTOR_REPORT.md`。

### 4.2 P2 建议删除(2 文件)— 待用户确认

| 文件 | 行数 | 与活文档的重叠 | 推荐 |
|---|---|---|---|
| `API_HANDOFF.md` | "API 交接文档",版本 1.9.0 | 与 `API.md` 完全同主题,前者是交接版本快照 | 删除 |
| `INTEGRATION.md` | "项目整合说明" | 与 `README.md` 启动 / 整合段落重叠 | 删除或合并到 README |

### 4.3 P2 建议归档/重写(2 文件)

| 文件 | 状态 | 建议 |
|---|---|---|
| `docs/analysis/rfc-002-historical-diagnostics.md` | RFC-002 设计稿,代码 `HistoricalDiagnosticsService` 已落地,2 个测试类锁定行为 | 删除(代码 + 测试是事实文档) |
| `docs/analysis/rfc-003-historical-quality-score.md` | RFC-003 设计稿,代码 `HistoricalQualityScorer` 已落地,2 个测试类锁定行为 | 删除 |
| `dataAnalyze/README.md` | 上一轮 audit 指出与代码不一致(将 simulate 描述为主路径) | **本 PR 不动**:rebuild.txt 范围与 clean.txt §3.2#5 冲突;留待 dataAnalyze 单独 RFC |

### 4.4 P3 暂缓(由用户裁决,见 §6.E)

| 文件 | 用途 | 不动理由 |
|---|---|---|
| `源代码说明文档.md` | 答辩 / 提交用 | 与 ARCHITECTURE.md 重叠但答辩需求未确认 |

### 4.5 验收

```bash
mvn -DskipFrontend=true test
cd sun && npm test -- --run
git status --short
```

后端 / 前端测试**与文档无关**,期望仍 468 / 62 全绿。

### 4.6 估算

- 默认删除:2 个 P1 + 4 个 P2 = **6 个 .md**
- 行数减少:估 ~1500 行 markdown
- 零代码改动,零测试改动

---

## 5. PR-CLEAN-5:测试命名 / 测试套收敛

**结论:本 PR 跳过(本轮不做)。**

理由:
- `CLEANUP_AUDIT.md §4.6` 列出的 4 个 `Pr9*Test` 文件都在引用现役 RFC-009 行为,**功能正活着**
- 单纯重命名收益低 + 风险:RFC 编号在文件名是定位 PR / 设计稿的关键索引,合并后丢失
- clean.txt §8.5 自己警告"不要在同一 PR 里同时做大规模逻辑删除和测试重命名",且当前所有逻辑删除候选都很轻 — 与本 PR 同台必无价值
- RFC-010A/B/C 文件命名同样有索引价值

**建议**:留作未来独立"测试套整理 RFC",**不放本轮**。

---

## 6. 用户裁决项(P3 暂缓)

下列 5 个分支需要用户明确指示。**不裁决前不动这些代码 / 文件**。

### 6.A Sync optimize 路径

- 涉及文件:
  - `src/main/java/com/bjtu/simulation/controller/SimulationOptimizationController.java`
  - `src/main/java/com/bjtu/simulation/service/OptimizationService.java`(部分,**异步路径还在用**)
  - `src/test/java/com/bjtu/simulation/controller/SimulationApiIntegrationTest.java` 第 265、303 行的 2 个 sync optimize 用例
- 现状:前端**不调用** sync `/optimize`(grep 0 命中);仅 1 个 controller 测试调用 sync 端点
- maintainer intent:`SimulationOptimizationAsyncController` javadoc 写"旧 SimulationOptimizationController **字面 0 改动以保留同步路径行为**" → **主动保留**
- **提问**:"sync `/api/simulation/optimize` 端点是否已废弃?如果是,我可以删除该 controller + sync test 用例,保留 `OptimizationService` 给异步路径用。"

### 6.B `samples/curated-history/`(81 跟踪 JSON + generator)

- 涉及文件:`samples/curated-history/manifest.json`、80 个 `summaries/curated-*.json`、`src/test/java/com/bjtu/simulation/tools/CuratedSamplesGenerator.java`
- 现状:无运行时 reader(grep 0 命中)。属于"答辩可演示历史 baseline"性质
- **提问**:"`samples/curated-history/` 是否仍是历史比对必须的数据集?如果是答辩交付物 → 保留。如果只是开发期产物 → 删除 81 文件 + generator。"

### 6.C `dataAnalyze/DiningSimulation.{cpp,h}`(C++ 死代码)

- rebuild.txt 上一轮已审计为"事实生产路径死代码",但属 C++ 子工程
- 现状:Java 不调 `--mode=simulate`,删除会牵动 vcxproj / CMakeLists.txt
- **提问**:"DiningSimulation 是否要在本轮一并删除?(上一轮 rebuild.txt 计划是单独 RFC)"

### 6.D RFC 测试文件命名

- 涉及文件:`Pr9b/c/d/e` 共 4 个测试类,RFC-010A/B/C 共 4 个测试类
- 现状:活测试,行为正确,仅命名带 RFC 编号
- **提问**:"本轮是否做测试重命名?(默认建议:不做)"

### 6.E `源代码说明文档.md`

- 现状:与 ARCHITECTURE.md 部分重叠
- **提问**:"该文档是否是答辩 / 提交必需?如果是 → 保留;如果不是 → 删除。"

---

## 7. 执行顺序

```
1. 用户审阅 CLEANUP_AUDIT.md + CLEANUP_PLAN.md(本文件)
2. 用户对 §6 五个分支给指令(可选,默认全 P3 暂缓 → 跳过)
3. 执行 PR-CLEAN-1(无争议)→ 跑测试 → 提交
4. 跳过 PR-CLEAN-2 / PR-CLEAN-3(无候选)
5. 执行 PR-CLEAN-4(文档,默认 6 文件)→ 跑测试 → 提交
6. 跳过 PR-CLEAN-5(本轮不做)
7. 若 §6 的某分支被 user 触发 → 各自单 PR
8. 阶段 4 最终验收 → 阶段 5 写 CLEANUP_REPORT.md
```

---

## 8. 红线自检(再次)

| 红线 | PR-CLEAN-1 | PR-CLEAN-4 | §6 暂缓项 |
|---|---|---|---|
| #1 无引用分析删生产代码 | ❌ | ❌ | 已等用户裁决 |
| #2 为旧测试保留废弃代码 | ❌ | ❌ | 6.A 让用户对此明确 |
| #5 修改业务行为 | ❌ | ❌ | 6.A 涉及行为(sync 端点),所以暂缓 |
| #6 把构建产物重提交 | 反向(把构建产物清理掉)| ❌ | ❌ |
| #7 保留"可能以后有用" | ❌ | ❌ | 6.B/6.E 让用户对此明确 |
| #11 一次性巨改 | 64 文件,单类(都是产物) | 6 文件 | 各分支单独 PR |
| #12 跳过最终测试 | 每 PR 后跑 mvn + npm test | 同 | 同 |

---

## 9. 数字小结(本计划默认执行部分)

| 项 | 数量 |
|---|---|
| PR-CLEAN-1 删除/移出索引文件 | 64 |
| PR-CLEAN-1 物理回收 | ~870 MB |
| PR-CLEAN-4 删除文档(默认) | 4 (P1 2 个 + P2 默认 2 个 `API_HANDOFF.md` + `INTEGRATION.md`) |
| PR-CLEAN-4 删除文档(扩展)| +2 (`docs/analysis/rfc-00{2,3}.md`) |
| PR-CLEAN-4 上限 | 6 |
| 后端代码改动 | **0** |
| 前端代码改动 | **0** |
| 测试代码改动 | **0** |
| 业务行为改动 | **0** |
